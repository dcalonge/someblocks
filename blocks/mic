#!/bin/sh

if pactl list sources | grep -q "Mute: yes"; then
  notify-send " Mic Muted" -t 500 -u low -c "osd-text"
else
  echo -n "  󰍬 "
  notify-send " Mic Unmuted" -t 500 -u low -c "osd-text"
fi
