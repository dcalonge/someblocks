#!/bin/sh

vol=$(pactl list sinks | grep 'Volume:' | head -n 1 | awk '{print $5}' | sed 's/%//')

if [ $(pactl list sinks | grep "Mute:" | awk '{print $2}') = "yes" ]; then
  echo -n "ﱝ"
else
  echo -n "$(/home/daniel/mis_repos/someblocks/blocks/percentage_volume $vol 󰕿 奔 󰕾)"
fi
